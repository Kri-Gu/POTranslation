# ──────────────────────────────────────────────────
# PO File Translator — Configuration
# ──────────────────────────────────────────────────
# Copy this to .env and fill in your values.
# .env is gitignored and never committed.

# OpenAI API Key (required)
OPENAI_API_KEY=sk-proj-YOUR-KEY-HERE

# ── Translation defaults ──────────────────────────
# These set the initial/default values for both the
# CLI and the Streamlit web UI. All can be overridden
# per-run via CLI flags or the sidebar.

# Target language: nb (Norwegian Bokmål), sv (Swedish), da (Danish)
# Also accepts aliases: no → nb, dk → da, se → sv
DEFAULT_TARGET_LANGUAGE=nb

# Source language detection: auto, en, de
DEFAULT_SOURCE_LANGUAGE=auto

# OpenAI model (see README for recommendations)
DEFAULT_MODEL=gpt-4.1

# Items per API call (5–50, lower = better quality)
BATCH_SIZE=20

# Path to domain context file (optional, leave empty to skip)
DEFAULT_CONTEXT_FILE=context.json
